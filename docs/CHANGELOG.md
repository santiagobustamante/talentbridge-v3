# Changelog — TalentBridge V3

Registro cronológico de cambios reales hechos al proyecto (no de features planeadas — eso vive en [`PHASES.md`](./PHASES.md) y [`NEXT_STEPS.md`](./NEXT_STEPS.md)). Cada entrada corresponde a una fase o tarea concreta. Las Fases 0–4.2 del plan de mejoras UX se hicieron en una sesión anterior a la creación de este archivo — su bitácora original y detallada sigue viva en [`plan-mejoras-frontend-ux.md`](./plan-mejoras-frontend-ux.md) (tabla "Bitácora de avance"), no se retranscribe acá para no duplicar la fuente de verdad; las entradas de abajo empiezan donde ese detalle ya está.

---

### 2026-07-16 — Fase 4.3: Unificación de render del portafolio
**Módulo:** Frontend — portafolio público/preview
**Tipo de cambio:** Refactor (eliminación de duplicación)
**Archivos modificados:** `shared/components/portfolio-content/*` (nuevo), `features/public-portfolio/*`, `features/public-preview/*`, `shared/components/github-warning/github-warning.component.ts`
**Problema:** `public-portfolio` y `public-preview` renderizaban el mismo contenido (hero+skills+experiencia+educación+proyectos+footer) con markup duplicado, 5 funciones de traducción duplicadas con una inconsistencia real (`levelClass` devolvía `level-high` en un lado y `high` en el otro — nombres de clase CSS distintos para el mismo dato), y cada uno copiaba a mano el aviso legal de GitHub.
**Solución:** Extraído `PortfolioContentComponent` compartido con input `editable` (muestra links "Editar sección" solo en la vista previa autenticada). Ambos componentes originales quedaron como wrappers delgados. Reusado `GithubWarningComponent` en los dos, y corregidos sus colores hardcodeados (no seguían los tokens `--info*`).
**Cómo se probó:** `npm run lint:css` + `ng build` limpios.
**Pendientes:** Ninguno para esta fase.

---

### 2026-07-16 — Fase 5: Bugs funcionales puntuales
**Módulo:** Frontend — landing, company-jobs, messages, profile, company-dashboard
**Tipo de cambio:** Fix
**Archivos modificados:** `features/home/home.component.scss`, `features/company/company-jobs.component.scss`, `features/messages/messages.component.ts`, `features/profile/profile.component.html`, `features/company/company-dashboard.component.{html,ts,scss}`, `features/auth/company-login.component.ts`, `features/company/company-candidates.component.ts`
**Problema:** 6 bugs del informe de auditoría: (1) placeholder del buscador de la landing cortado visualmente, (2) grid de la tabla de ofertas con 8 columnas definidas para 9 reales en el breakpoint intermedio, (3) `console.error` de depuración en `messages.component.ts`, (4) URL pública hardcodeada `"localhost:4200"` en el resumen de perfil, (5) `company-dashboard` sin fallback de logo (ni estilos ni rama `@else`), (6) el parámetro `q` del buscador de la landing se perdía en el camino hacia `company/login`.
**Solución:** Fixes puntuales por archivo. El bug (6) resultó tener **dos causas adicionales no documentadas en el hallazgo original**: `company-login` nunca leía el `q` de la URL para reenviarlo tras login, y `company-candidates` tampoco leía `q` de su propia ruta ni disparaba búsqueda al cargar — se corrigieron los tres puntos de la cadena.
**Cómo se probó:** `npm run lint:css` + `ng build` limpios. Verificación visual en navegador real pendiente (ver Fase 11 en `NEXT_STEPS.md`).
**Pendientes:** Confirmación visual del usuario en navegador real.

---

### 2026-07-16 — Fase 6: Estados vacíos, manejo de errores y confirmaciones
**Módulo:** Frontend — dashboards, mensajes, portafolio (skills/experiencia/educación/proyectos), company-jobs
**Tipo de cambio:** Feature (componente compartido nuevo) + Fix (manejo de errores faltante)
**Archivos modificados:** `shared/components/empty-state/*` (nuevo), `shared/components/confirm-dialog.component.ts`, `features/home-candidate/*`, `features/company/company-dashboard.*`, `features/messages/messages.*`, `features/company/company-jobs.component.ts`, `features/{skills,experiences,education,projects}/*.component.ts`
**Problema:** Estados vacíos como texto plano en 6 puntos (se encontró un tercer caso en `company-dashboard` — vacantes recientes — no listado en el hallazgo original de auditoría). 4 métodos `remove()` (Habilidades/Experiencia/Educación/Proyectos) y el `save()` de Educación llamaban `.subscribe(next)` sin callback `error` — un fallo de red quedaba silencioso para el usuario. `company-jobs` usaba `confirm()` nativo del navegador para cerrar/archivar/eliminar oferta; `messages` tenía un diálogo de bloqueo de conversación hecho a mano (overlay + CSS propio) en vez del componente compartido.
**Solución:** Nuevo `EmptyStateComponent` (ícono+título?+texto+acción?, variante `compact`). Agregado callback `error` con snackbar en los 5 puntos que faltaban. Reemplazados los 4 `confirm()` y el diálogo hecho a mano por `ConfirmDialogComponent`, extendido con `confirmLabel`/`confirmColor` opcionales para no forzar el texto "Eliminar" en acciones no destructivas.
**Cómo se probó:** `npm run lint:css` + `ng build` limpios.
**Pendientes:** Ninguno para esta fase.

---

### 2026-07-16 — Fase 7: Fecha y traducciones unificadas
**Módulo:** Frontend — global (`app.config.ts`) + 11 componentes que mostraban fechas o estados
**Tipo de cambio:** Fix (bug de fondo no documentado) + Refactor
**Archivos modificados:** `app.config.ts`, `shared/pipes/app-date.pipe.ts` (nuevo), `shared/utils/format-date.util.ts` (nuevo), `shared/components/badge/status-label.util.ts` (nuevo), `features/{cv-analysis,jobs,experiences,education,company-view,company/company-dashboard,messages,company/company-jobs,home-candidate}/*`, `shared/components/portfolio-content/*`, `features/profile/*`, `features/projects/*`
**Problema:** Bug de fondo encontrado durante esta fase, **no estaba en la auditoría original**: Angular nunca tuvo `LOCALE_ID` configurado en `app.config.ts`, así que el pipe `date` nativo (y cualquier formato `longDate`/`medium`/`shortTime`) renderizaba en **inglés** (`en-US`, default de Angular) en toda una aplicación 100% en español — solo los `toLocaleDateString('es-CO', ...)` manuales escapaban al bug. Además, 8 formatos de fecha distintos convivían sin criterio, y el mapping estado→texto (`DRAFT`→"Borrador", etc.) estaba copiado 6 veces (2 de ellas duplicadas dentro del mismo archivo, `candidate-jobs.component.ts`).
**Solución:** Registrado `LOCALE_ID: 'es-CO'` + `registerLocaleData` globalmente. Creados `formatAppDate`/pipe `appDate` (5 variantes: `short/long/datetime/time/monthYear`) y `statusToLabel` (complementa `statusToTone`) como fuentes únicas.
**Cómo se probó:** `npm run lint:css` + `ng build` limpios.
**Pendientes:** Ninguno para esta fase.

---

### 2026-07-16 — Fase 8: Flujo guiado del candidato (feature nueva)
**Módulo:** Frontend — dashboard candidato, ofertas de trabajo
**Tipo de cambio:** Feature nueva
**Archivos modificados:** `shared/components/profile-checklist/*` (nuevo), `features/home-candidate/*`, `features/jobs/candidate-jobs.{component.ts,component.html,component.scss}`
**Problema:** El progreso de completitud del perfil se mostraba como "100%" en texto plano; el indicador de coincidencia de habilidades con una oferta era una frase ("Coincide con tu perfil: X, Y") sin ninguna señal visual de magnitud.
**Solución:** `ProfileChecklistComponent` con anillo de progreso SVG + 4 ítems accionables, replicando exactamente la regla de completitud real del backend (`candidate-service/profile.service.ts`) en vez de aproximarla. Indicador de match convertido a barra + porcentaje, usando datos que la API ya devolvía (`requiredSkillsList`/`matchedSkills`) sin tocar backend.
**Cómo se probó:** `npm run lint:css` + `ng build` limpios.
**Pendientes:** Ninguno para esta fase.

---

### 2026-07-16 — Fase 10: Adopción final de componentes compartidos
**Módulo:** Frontend — auth (4 formularios), company-view, company-candidates
**Tipo de cambio:** Refactor (eliminación de duplicación de CSS)
**Archivos modificados:** `features/auth/{login,register,company-login,company-register}.component.{ts,scss}`, `features/company-view/*`, `features/company/company-candidates.{component.ts,component.html,component.scss}`, `FRONTEND/src/styles/README.md`
**Problema:** Los 4 formularios de auth usaban `mat-raised-button` de Angular Material + una clase `.submit-btn` con **7 propiedades `!important`** solo para forzar que se viera como el botón del sistema de diseño — reimplementación duplicada del mismo componente, 4 veces. `company-view` tenía `.btn-primary`/`.btn-secondary`/`.retry-btn` hechos a mano (literalmente nombrados como las variantes de `appButton` pero sin usarlo).
**Solución:** Migrados los 4 formularios de auth y `company-view` a `[appButton]`, eliminando el CSS `!important` y el import de `MatButtonModule` donde quedó sin uso. Evaluados y **descartados a propósito** (no por omisión): los botones de ícono del chrome de navegación de `company-shell` (patrón visual distinto, no CTA con texto) y `.view-btn`/`.btn-clear` de `company-candidates` (ninguna variante de `appButton` reproduce su estilo real sin regresión visual). `Card` compartido: confirmado que sigue en cero usos reales — documentado como pendiente explícito en vez de forzar un sweep mecánico sobre ~14 archivos sin poder verificar visualmente cada uno.
**Cómo se probó:** `npm run lint:css` + `ng build` limpios.
**Pendientes:** Adopción de `Card` (ver `NEXT_STEPS.md`).

---

### 2026-07-16 — Bootstrap de documentación del proyecto
**Módulo:** Documentación (`docs/`, raíz)
**Tipo de cambio:** Documentación
**Archivos modificados:** Creados `docs/PROJECT_OVERVIEW.md`, `docs/ARCHITECTURE.md`, `docs/FRONTEND_GUIDE.md`, `docs/BACKEND_GUIDE.md`, `docs/DATABASE.md`, `docs/CHANGELOG.md`, `docs/DECISIONS.md`, `docs/BUGS_AND_FIXES.md`, `docs/TESTING_CHECKLIST.md`, `docs/REUSABLE_SKILLS.md`, `docs/PROMPTS.md`, `docs/NEXT_STEPS.md`, `docs/PHASES.md`, `CLAUDE.md`.
**Problema:** El proyecto no tenía un set de documentación operativa/de trazabilidad — solo documentos de planificación puntual (`plan-mejoras-frontend-ux.md`, `arquitectura.md`, `endpoints.md`, etc.), sin un punto de entrada único, sin registro estructurado de bugs/decisiones, y sin instrucciones para que un agente IA retome el proyecto sin perder contexto.
**Solución:** Bootstrap completo del set de documentación pedido, grounded en el código real (schema Prisma, `docker-compose.yml`, guards, DTOs, seeds) — no plantillas genéricas.
**Cómo se probó:** N/A (documentación, no código ejecutable).
**Pendientes:** Mantener actualizado en cada tarea futura — ver `CLAUDE.md`, sección "Checklist obligatorio antes de terminar una tarea".

---

### 2026-07-17 — Auditoría en vivo + correcciones puntuales de presentación
**Módulo:** Frontend — sidebar candidato (`AppShellComponent`), tabla de vacantes (`company-jobs`), búsqueda de candidatos (`company-candidates`), chips de coincidencia (`candidate-jobs`), ambos shells
**Tipo de cambio:** Auditoría + Fix
**Archivos modificados:** `shared/layout/app-shell.component.ts`, `shared/layout/app-shell.component.scss`, `features/company/company-shell.component.scss`, `features/company/company-jobs.component.{scss,html}`, `features/company/company-candidates.component.html`, `features/jobs/candidate-jobs.component.html`
**Problema:** Se pidió una revisión y corrección exhaustiva de ~40 módulos del frontend para dejarlo listo para presentación de grado. En vez de reescribir a ciegas, se levantó la app real (Docker + 10 microservicios + frontend, ya estaban corriendo) y se recorrió con un navegador automatizado (Playwright + Edge local, sin dependencias nuevas de red) como candidato y como empresa, comparando pantalla por pantalla contra el pedido. Resultado: la gran mayoría de lo pedido ya estaba resuelto (de la sesión anterior o de trabajo de backend sin commitear que no se sabía que existía — paginación/filtros de postulaciones, `logoUrl`/`workload`/`customContractType` en 5 endpoints, 11 logos SVG reales ya cargados en la base). Bugs reales confirmados: menú lateral del candidato duplicando texto, columna Contrato truncada en la tabla de vacantes, mayúsculas inconsistentes en chips de habilidades. Dos "bugs" reportados como hipótesis (landing con huecos en blanco, Joaquín tapando contenido en mobile) resultaron ser artefactos de la herramienta de captura de pantalla, no bugs reales — descartados tras verificar con scroll real + captura de viewport.
**Solución:** Ver BUG-009, BUG-010, BUG-011 en `BUGS_AND_FIXES.md` para el detalle de cada fix. Agregado además `padding-bottom` defensivo en `.page-content` de ambos shells como margen de seguridad para el botón flotante de Joaquín, aunque no se confirmó que hiciera falta.
**Cómo se probó:** `npm run lint:css` + `ng build` limpios. Cada fix verificado visualmente contra el servidor de desarrollo real (no solo contra el build), incluyendo una segunda iteración cuando el primer ajuste de la tabla de vacantes truncó el encabezado "Modalidad".
**Pendientes:** Ver `NEXT_STEPS.md` — incluye el hallazgo de que todas las ofertas de `empresa001@demo.com` tienen `workload: null` (columna "Jornada" vacía en la tabla, dato de seed incompleto, no bug de código). *(resuelto más tarde el mismo día, ver entrada siguiente)*

---

### 2026-07-17 (continuación) — Backfill de jornada + bug real de mensajes duplicados en chat
**Módulo:** Backend/datos (`prisma/`), Frontend — `messages`
**Tipo de cambio:** Backfill de datos + Fix
**Archivos modificados:** `BACKEND/prisma/backfill-job-workload.ts` (nuevo), `BACKEND/package.json`, `FRONTEND/src/app/features/messages/messages.component.ts`
**Problema:** (1) Se confirmó que **las 66 ofertas publicadas de toda la base** (no solo las de `empresa001@demo.com`) tenían `workload: null` — un vacío de seed sistémico, no puntual. (2) Continuando la auditoría en vivo con una prueba real de envío de chat, se descubrió un bug genuino (no falso positivo esta vez): al enviar un mensaje, el remitente lo veía duplicado 2-3 veces en pantalla — ver BUG-012.
**Solución:** (1) Script `backfill-job-workload.ts` (mismo patrón que `update-company-logos.ts`: `UPDATE` idempotente, solo toca filas con `workload: null`, no borra ni crea nada) — corrido contra la base, 68 ofertas actualizadas (heurística: "Prácticas" → "Medio tiempo", el resto → "Tiempo completo"). Agregado como script npm `backfill:job-workload` para poder re-correrlo si se agregan más ofertas de seed sin este campo. (2) Ver BUG-012 en `BUGS_AND_FIXES.md` para el fix del chat.
**Cómo se probó:** Backfill verificado con `curl` directo al endpoint de ofertas antes/después. Fix de chat verificado con una prueba real de envío de mensaje vía navegador automatizado, contando los elementos reales del DOM (no el texto de la página completa) antes y después del fix. `npm run lint:css` + `ng build` limpios. `npx prisma validate` + `npx prisma generate` corridos (no hubo cambios de schema, solo de datos). Los mensajes de prueba usados para detectar y verificar el bug de chat se borraron de la conversación demo real al terminar — no eran datos originales, eran contaminación introducida por las propias pruebas de esta sesión.

---

### 2026-07-16 — Fase 0.1: Limpieza de habilidades del perfil demo (Santiago)
**Módulo:** Backend/datos — perfil `bustamantemolinasantiago@gmail.com` (`profiles.id = 101`)
**Tipo de cambio:** Limpieza de datos (autorizada explícitamente por el usuario en esta sesión)
**Archivos modificados:** Ninguno de código — cambio de datos directo en `smart_portfolio` (contenedor `smart_portfolio_db_v3`, proyecto Docker `version3`), tabla `skills`.
**Problema:** `NEXT_STEPS.md`/`DATABASE.md` documentaban ~150 habilidades cargadas (todo el catálogo) como pendiente de limpieza. Al verificar el estado real antes de tocar nada, el conteo actual era 13, no 150 — la cifra de la documentación estaba desactualizada. Las 13 eran una mezcla de las 4 habilidades originales del seed que sobrevivieron (Angular, NestJS, PostgreSQL, Docker — `TypeScript` y `Git y GitHub` ya no estaban) más 7 agregadas manualmente durante auditorías en vivo de sesiones anteriores (Linux, HTML5, Functional Programming, SQL, HTML, Jira, React, Django) y un artefacto de prueba explícito (`Prueba-Custom-Skill-1784260400168`, creado 2026-07-17 durante una prueba automatizada del flujo de agregar habilidad).
**Solución:** Se pidió confirmación puntual al usuario (no alcanzaba la autorización general del plan) y se obtuvo un "sí" explícito. Se respaldaron las 13 filas originales antes de borrar (ver `DECISIONS.md`, entrada de esta misma fecha, para dónde queda el respaldo). Se hizo `DELETE` de las 13 filas y se insertaron las 6 habilidades que ya define `BACKEND/prisma/seed-santiago-profile.ts` (Angular, NestJS, TypeScript, PostgreSQL, Docker, Git y GitHub — el set que el propio script del proyecto considera "realista" para este perfil, coincide con el `summary` del perfil). No se corrió el script de seed completo porque también resetea experiencias/educación/proyectos/chat, y esos datos no formaban parte de lo autorizado.
**Cómo se probó:** Verificación por query SQL directa contra `smart_portfolio_db_v3` antes y después del cambio (conteo y listado de `skills` para el perfil). No requiere build (no se tocó código).
**Pendientes:** Ninguno para este ítem — cierra `NEXT_STEPS.md` #1 y Fase 0.1 de `plan-mejoras-frontend-ux.md`/`PHASES.md`.

---

### 2026-07-16 — Fase 13: Rediseño del registro de habilidades + matching por nivel
**Módulo:** Frontend — `skills`, `jobs` (candidato y empresa) · Backend — `jobs-service`, `applications-service`, `libs/contracts`
**Tipo de cambio:** Feature (rediseño visual + regla de negocio nueva, opcional y no disruptiva)
**Archivos modificados:** `shared/components/level-meter/*` (nuevo), `features/skills/skills.component.*`, `features/jobs/candidate-jobs.component.*`, `features/company/company-jobs.component.*`, `core/models/jobs.models.ts`, `BACKEND/libs/contracts/src/skill-match.util.ts` (nuevo) + `index.ts`, `BACKEND/apps/jobs-service/src/jobs.service.ts`, `BACKEND/apps/applications-service/src/applications.service.ts`.
**Problema:** (1) El usuario encontró el flujo de agregar habilidades "impreciso y feo visualmente": el nivel (Básico/Intermedio/Avanzado/Experto) se repetía de 3 formas redundantes en cada tarjeta (badge de texto + barra + 4 puntos), y el catálogo de ~620 habilidades usaba un acordeón por categoría que había que expandir una por una. (2) El matching candidato↔oferta era binario por presencia de nombre (alcanza con 1 coincidencia, sin mirar nivel) — el usuario había aprobado un mockup de matching más preciso que sí compara nivel requerido vs. nivel del candidato.
**Solución:** (1) Nuevo `LevelMeterComponent` compartido (barra segmentada de 4 pasos + descripción corta de qué significa cada nivel) que reemplaza las 3 representaciones redundantes, reusado en agregar/agregar en lote/editar en línea. Catálogo cambiado de acordeón a: buscador de texto (gana siempre) + chips de categoría de un solo nivel que filtran una grilla plana (sin anidar expand/collapse). Decisión de mantener las 4 categorías de nivel sin cambios (consultado con el usuario) — solo cambió la presentación, no el dato. (2) Nuevo `computeSkillMatch` en `libs/contracts` (compartido entre `jobs-service` y `applications-service`): admite opcionalmente `"Nombre:NIVEL"` por habilidad requerida (ej. `"Angular:ADVANCED"`) sin migrar el schema — las entradas sin `:NIVEL` siguen significando "cualquier nivel sirve", 100% retrocompatible con las ~200 ofertas de seed existentes. El `matchPercent` mostrado al candidato ahora es honesto con el nivel; la regla de elegibilidad para postular (`hasAnyMatch`) se dejó sin cambios (presencia de nombre, no nivel) por decisión explícita del usuario — no bloquea a nadie que hoy podía postularse. Se agregó el control de nivel mínimo opcional al formulario de oferta de la empresa (`company-jobs`), y un desglose por habilidad (cumple/nivel insuficiente/te falta) en las tarjetas de oferta del candidato.
**Bug encontrado y corregido en la misma sesión:** al verificar en vivo, el preview de chips de habilidades de la tarjeta de oferta (`visibleSkills()`) y el modal de detalle mostraban el string crudo de almacenamiento (`"Angular:ADVANCED"`) en vez de solo `"Angular"` — filtraba un detalle de implementación interna al candidato. Corregido con un helper `stripRequiredLevel()` aplicado en los dos puntos donde se listaba `skillsRequired` fuera del desglose de matching (que ya mostraba el nombre limpio por separado).
**Cómo se probó:** `ng build` + `lint:css` limpios (sin warnings nuevos). `nest build` limpio para `jobs-service`, `applications-service` y los 5 libs compartidos. Verificado en vivo contra el servidor real: candidato agregando habilidades por catálogo/categoría/búsqueda y editando nivel en línea (perfil demo, ver `DECISIONS.md`), empresa editando la oferta real `id=12` ("Ingeniero de Software Junior", Talento Llanero S.A.S.) para exigir Angular en nivel Avanzado, y el candidato viendo el desglose resultante (`Angular · Nivel insuficiente`, 29% de coincidencia, seguía pudiendo postularse). Confirmado por request/response de red, no solo por inspección visual.
**Pendientes:** Ninguno. La oferta `id=12` quedó con el requisito de nivel real cargado (no es dato de prueba a limpiar, es una demostración legítima de la feature en una cuenta demo).

---

### 2026-07-17 — Fase 14: Español colombiano (sin voseo), sugerencias desde el CV y aval de empresas
**Módulo:** Frontend — `skills`, `cv-analysis`, `company-candidates`, `level-meter`, `company-jobs` (copy) · Backend — `portfolio-service` (`skills.service.ts`, `skills.controller.ts`), `company-service` (`candidate-search.service.ts`), schema (`SkillEndorsement`)
**Tipo de cambio:** Corrección de copy (regionalismo) + 2 features nuevas de las 3 propuestas en el mockup anterior (la tercera, matching más preciso, ya se había implementado en la Fase 13)
**Problema:** (1) El usuario, colombiano, señaló que parte del copy de `skills` usaba voseo rioplatense ("Elegí", "escribí") en vez de tuteo estándar — pidió corregirlo en todo el proyecto y mantenerlo así de forma permanente. (2) Pidió implementar las 3 opciones mostradas en el mockup de la sesión anterior: matching inteligente (ya hecho), aval de empresas sobre una habilidad puntual, y sugerencias de habilidades detectadas en el CV.
**Solución:**
- **Español sin voseo:** barrido completo del proyecto (frontend + backend) buscando conjugaciones voseo reales (no solo palabras con tilde en "s"). Corregidas 9 instancias reales: `skills.component.html` (3), `home-candidate.component.ts` (4), `profile-checklist.component.html` (1), y dos que el propio agente escribió en esta misma sesión sin darse cuenta — `level-meter.component.ts` (2 hints de nivel) y `company-jobs.component.html` (1 texto de ayuda). Guardado como preferencia permanente en memoria (`feedback_colombian_spanish.md`).
- **Sugerencias desde el CV:** sin cambios de backend — `CvDocument.extractedText` ya viajaba al frontend sin usarse. `cv-analysis.component.ts` ahora compara ese texto (tras analizar) contra `SKILL_CATALOG`, excluye lo que el candidato ya tiene, y ofrece un checklist con contador en vivo que agrega en lote reusando el mismo patrón de `forkJoin` de `skills.component`. Bug encontrado y corregido en la misma sesión: la primera versión usaba `.includes()` (substring plano), que daba falsos positivos con nombres de catálogo cortos (`C`, `R`, `Go`, `iOS`, `LAN`, `SPA` — ej. "C" coincidía dentro de "C1", el nivel de inglés del CV real). Corregido con un regex de límite de palabra (`\b...\b`).
- **Aval de empresas:** nueva tabla `SkillEndorsement` (`skillId`, `companyId`, único por par) — aditiva, sin tocar columnas existentes. Nuevos endpoints `POST`/`DELETE /api/skills/:id/endorse` (guardados con `RolesGuard`+`@Roles(COMPANY)`), con la regla de negocio ya definida en el mockup: solo una empresa con una `Conversation` o `JobApplication` previa con el candidato puede avalar. `candidate-search.service.ts` ahora expone `id`/`endorsementCount`/`endorsedByMe` por habilidad; `company-candidates` tiene un botón "Avalar" por chip; `skills.component` muestra "Avalada por [Empresa]" de forma read-only.
**Incidente evitado durante el trabajo:** `npx prisma migrate dev` detectó que el historial de migraciones está desalineado con la base real (drift preexistente, de antes de esta sesión — probablemente por cambios de schema aplicados con `db push` en vez de migraciones formales en sesiones anteriores) y pidió un **reset completo de la base** (borra todo). Se abortó esa vía sin ejecutarla y se usó `npx prisma db push` en su lugar, que sincronizó solo la tabla nueva sin tocar nada existente — verificado con conteos de filas antes/después (162 usuarios, 68 ofertas, 949 habilidades, sin cambios). Ver `DECISIONS.md` y `REUSABLE_SKILLS.md`.
**Cómo se probó:** `ng build` + `lint:css` limpios. `nest build` limpio en `portfolio-service`, `company-service`, `jobs-service`, `applications-service`. Verificado en vivo: (a) copy sin voseo visible en `/app/skills`; (b) análisis real del CV de Santiago detectando 18 tecnologías reales no presentes en su perfil (Vue.js, HTML5, Python, Git, Arduino, etc.), sin falsos positivos tras el fix; (c) `empresa001@demo.com` avalando "Angular" del perfil de Santiago (201, verificado por red) y viéndolo reflejado en `/app/skills`; (d) `empresa002@demo.com` (sin relación con Santiago) recibiendo 403 con mensaje claro al intentar avalar la misma habilidad.
**Pendientes:** Ninguno.
**Pendientes:** Ninguno para esta tarea puntual.

---

### 2026-07-17 — Rediseño del flujo de agregar habilidades: catálogo por categorías + búsqueda rápida
**Módulo:** Frontend — `skills` · Backend — `portfolio-service` (`SkillDto`, `SkillsService`)
**Tipo de cambio:** Feature (rediseño de UX) + Fix (2 bugs descubiertos durante la verificación en vivo)
**Archivos modificados:** `FRONTEND/src/app/core/services/skill-catalog.ts` (agregado, sin tocar lo existente), `FRONTEND/src/app/features/skills/skills.component.ts` (reescrito), `FRONTEND/src/app/features/skills/skills.component.html` (nuevo — el componente pasó de `template:` inline a `templateUrl`), `FRONTEND/src/app/features/skills/skills.component.scss`, `BACKEND/apps/portfolio-service/src/dto/skill.dto.ts`, `BACKEND/apps/portfolio-service/src/skills.service.ts`
**Problema:** El flujo anterior para agregar habilidades era un formulario de autocompletar de a una habilidad por vez (escribir, elegir sugerencia, elegir nivel, enviar, repetir) — lento para un catálogo de 635 habilidades en 21 categorías, y no daba ninguna vista general de qué categorías existen o cuáles ya están cargadas.
**Solución:** Nuevo `groupCatalogByCategory()` en `skill-catalog.ts` (agrupa y ordena el catálogo existente por categoría, filtra por búsqueda — no modifica `filterCatalog()`, que siguen usando experiencias y proyectos). Componente `skills` reescrito con: acordeón de las 21 categorías con contador de chips por categoría, selección múltiple por click (chips con estado seleccionado/ya-agregada), barra de selección con nivel de partida único + botón "Agregar" que crea todas las seleccionadas en batch (`forkJoin` + `catchError` por ítem, para que un fallo puntual no aborte el resto), buscador que filtra categorías y chips a la vez, formulario de habilidad personalizada como respaldo (para nombres fuera del catálogo), y edición de nivel en línea sobre cada tarjeta ya agregada (reemplaza el formulario superior de edición que existía antes). Se descartaron las otras 2 opciones de diseño propuestas (lista plana con filtro de categoría; buscador tipo "chips a medida que se escribe" sin agrupación) — el usuario eligió explícitamente esta ("Catálogo por categorías + búsqueda rápida") de 3 alternativas presentadas.
**Errores encontrados durante la verificación en vivo (no en el diseño original, surgieron al probar el flujo real):** Ver BUG-013 (backend rechazaba `PATCH` de solo-nivel por DTO mal tipado) y BUG-014 (flexbox sin `flex-shrink: 0` colapsaba las filas de categoría a ~14px) en `BUGS_AND_FIXES.md`.
**Cómo se probó:** `npx ng build` limpio (solo warnings de tamaño de bundle ya conocidos). `npx prisma validate` limpio. `npm run build:portfolio` limpio tras el fix de BUG-013. Verificado en vivo con Playwright + Edge contra el stack real corriendo (Docker + 10 microservicios + `ng serve`): login candidato → búsqueda en catálogo → expandir/colapsar categorías → selección múltiple de chips → agregar en batch → edición de nivel en línea → habilidad personalizada. Los 4 registros de prueba creados durante la verificación (ids 1000-1003: Express.js, Node.js, un custom-skill con timestamp, Spring Boot) se borraron al terminar vía `DELETE` directo al endpoint, confirmando que las 11 habilidades originales del perfil demo (Angular, NestJS, PostgreSQL, Docker, Linux, HTML5, Functional Programming, SQL, HTML, Jira, React) quedaron intactas con sus niveles originales.
**Pendientes:** Ninguno para esta tarea. Nota: `filterCatalog()` (usado por experiencias y proyectos) no fue modificado a propósito — ver `DECISIONS.md`.

---

### 2026-07-17 (continuación) — Saludo con nombre de empresa + logos únicos por empresa
**Módulo:** Frontend — `company-shell` (header) · Backend/datos — `company_profiles.logo_url`, `FRONTEND/public/assets/company-logos/`
**Tipo de cambio:** Fix de UX + regeneración de datos (autorizada por el pedido explícito del usuario, "cambiar los logos")
**Problema:** (1) El usuario mostró una captura del perfil empresarial: el saludo del header decía "Hola, empresa001@demo.com" (el email) en vez del nombre de la empresa. (2) El avatar junto al nombre de la empresa mostraba una "M" en un cuadrado morado — al investigar, los "11 logos reales" que una sesión anterior había documentado como completos (Fase 9 en `PHASES.md`) resultaron ser la misma silueta abstracta genérica (un garabato que se parece a una M, no una M real) repartida sin variación entre 5-6 empresas cada una — 61 empresas, solo 11 archivos, la mayoría compartidos.
**Solución:** (1) `company-shell.component.ts` ahora pide `CompanyService.getProfile()` al iniciar y el header muestra `companyName` (con fallback al email si el perfil no cargó) — el avatar del header también pasó a mostrar el logo real si existe, no solo la inicial. (2) Nuevo script `BACKEND/prisma/generate-company-logos.ts` (`npm run generate:company-logos`): genera una marca abstracta única por empresa combinando 22 siluetas (hoja, rayos de sol, circuito, nube, barras, hexágono, escudo, pulso, molécula, engranaje, puente, edificio, balanza, libro, megáfono, ruta, carrito, grilla, brújula, órbita, infinito) con 22 colores, sin repetir la combinación entre las 61 empresas, con preferencia temática por sector cuando aplica (agro→hoja, salud→pulso, finanzas→barras, construcción→puente, legal→balanza, etc.). Los 11 archivos compartidos del set anterior se eliminaron al quedar huérfanos.
**Cómo se probó:** `ng build` + `lint:css` limpios. Verificado en vivo: `empresa001@demo.com` ahora ve "Hola, Talento Llanero S.A.S." en el header, con su logo nuevo (`logo-1.svg`, ícono de circuito en azul) cargando correctamente (confirmado por `naturalWidth` del `<img>`, no solo por inspección visual) tanto en el header como en `/company/profile`. Confirmado por consulta directa a la base: 61 empresas, 61 `logo_url` distintos (antes: 11 valores compartidos entre 61 filas).
**Pendientes:** Ninguno. La asignación sector→ícono es una preferencia de mejor esfuerzo (usa el campo `sector`, no el nombre de la empresa) — no se buscó una correspondencia perfecta para cada una de las 61 empresas, es una mejora de variedad visual sobre el estado anterior (una sola silueta repetida), no una curaduría manual por empresa.

---

### 2026-07-17 (continuación) — Mismo fix de saludo, ahora en el shell del candidato
**Módulo:** Frontend — `app-shell` (header candidato)
**Tipo de cambio:** Fix de UX (paridad con el fix de `company-shell` del mismo día)
**Problema:** El usuario pidió aplicar el mismo cambio de saludo ("nombre, no correo") al lado candidato, que tenía el mismo problema que el lado empresa: `Hola, {{ userEmail }}`.
**Solución:** A diferencia de `company-shell` (que necesitó un fetch nuevo a `CompanyService.getProfile()`), acá no hizo falta ningún request adicional — `/api/auth/me` (consumido por `AuthService.fetchMe()`, ya llamado al iniciar la app) ya incluye `profile` con `include: { profile: true, companyProfile: true }` en el backend. Solo se agregó un getter `greetingName` en `app-shell.component.ts` que devuelve `auth.currentUser()?.profile?.fullName`, con fallback al email si el perfil no tiene nombre cargado.
**Alcance:** A propósito, solo el texto del saludo — el usuario aclaró explícitamente "me refiero a lo del saludo" para distinguirlo del cambio de logos (que no aplica a candidatos). No se tocó el avatar del header (sigue mostrando la inicial), a diferencia de lo que sí se hizo en `company-shell`.
**Cómo se probó:** `ng build` + `lint:css` limpios. Verificado en vivo: login como `bustamantemolinasantiago@gmail.com` → header muestra "Hola, Santiago Bustamante Molina".
**Pendientes:** Ninguno.

---

### 2026-07-17 (continuación) — Ronda de fixes visuales + bug real de "vistas de perfil" nunca implementado
**Módulo:** Frontend — `candidate-jobs`, `experiences`, `portfolio-content`, `profile`, `company-profile`, `company-register` · Backend — `portfolio-service` (`public-portfolio.controller.ts`/`.service.ts`)
**Tipo de cambio:** Fixes de UX + un bug de backend genuino encontrado y corregido (feature documentada pero nunca conectada)
**Problema y solución, por ítem:**
1. **Chips "Te falta" poco distinguibles** — usaban gris apagado sobre el fondo verde del bloque de match, casi invisibles. Cambiados a `--danger-soft`/`--danger-strong` (rojo), distinto de verde=cumple y ámbar=nivel insuficiente.
2. **Campo "Tipo de contrato" del formulario de experiencia con disposición rara** — quedaba solo en una grilla de 2 columnas con 5 campos (huérfano en la última fila). Se le agregó `grid-column: 1 / -1` para que ocupe la fila completa.
3. **Línea del timeline de experiencia "rota"** — era un único `div` absoluto que atravesaba TODO el contenedor (`top:0; bottom:0`), incluida la tarjeta final completa, en vez de terminar en el último punto. Rediseñado: cada ítem dibuja su propio conector hacia el siguiente vía `::before` (con `top`/`bottom` relativos al propio borde del ítem, no a su alto real, así funciona sin importar cuánto texto tenga la tarjeta), y el último ítem simplemente no dibuja conector — sin necesidad de "tapar" la línea con un color de fondo. Mismo fix aplicado en `portfolio-content` (vista pública), que tenía el mismo patrón y el bug era más notorio ahí (línea completa atravesando toda la última tarjeta, no solo un margen).
4. **Autocompletado y formato de teléfono/NIT** — `profile.component` y `company-profile.component` tenían `autocomplete="off"` puesto a propósito en el `<form>` completo, desactivando el autocompletado del navegador para todos los campos. Quitado, y agregados tokens estándar por campo (`name`, `tel`, `url`, `address-level2`, `organization`). Nuevo `formatColombianPhone()`/`formatColombianNit()` (`shared/utils/phone-format.util.ts`) enganchados vía `valueChanges` de cada `FormControl` — el teléfono se reformatea en vivo mientras se escribe ("+57 300 123 4567"), igual el NIT ("900.123.456-7"). Alcance: los 2 formularios de perfil (candidato y empresa) + 2 campos sueltos en `company-register` — no se auditó *toda* la app campo por campo dado el tamaño del proyecto.
5. **"Vistas de tu perfil" nunca se registraba — bug real, no solo de UI:** existía el modelo `ProfileView`, un endpoint `GET /profile/views` para que el candidato consulte el conteo, y hasta el stat en el dashboard — pero **nada en todo el backend llamaba jamás a `profileView.create()`**. La ruta pública `GET /portfolio/:slug` (la que usa la empresa al hacer clic en "Ver" desde `company-candidates`) no tenía ningún guard, así que no había forma de saber quién la visitaba. Corregido: se le agregó `OptionalJwtAuthGuard` (ya existía en `@app/auth`, no se creó de cero) y `getBySlug()` ahora registra una `ProfileView` cuando el visitante es una empresa autenticada y no es su propio perfil, con un límite de 10 minutos para no inflar el conteo con refrescos de la misma visita.
**Bug encontrado en el camino (dentro del mismo fix):** la primera versión usaba el decorador `@CurrentUser()` para leer el visitante opcional — pero ese decorador **lanza 401 si no hay sesión**, lo cual rompe el acceso anónimo real a un portafolio público (todo el sentido de `OptionalJwtAuthGuard`). Corregido usando `@Req() req` y leyendo `req.user` directamente, sin el decorador estricto. Se corrigió el mismo patrón en `previewMyPortfolio` (mismo archivo) por consistencia, aunque en la práctica nunca se llama de forma anónima.
**Incidente de infraestructura durante la verificación:** varios microservicios backend (los arrancados en sesiones anteriores) se habían caído en algún punto de esta sesión larga sin que se notara — se reiniciaron todos. Además se confirmó que `nest start --watch` en un proceso lanzado en segundo plano **no siempre recompila solo** al guardar un archivo (contrario al comportamiento esperado) — el primer intento de fix de este mismo bug pareció no funcionar en vivo simplemente porque el proceso seguía sirviendo el código viejo. Ver `REUSABLE_SKILLS.md`.
**Cómo se probó:** `ng build` + `lint:css` + `nest build` (portfolio-service) limpios. Verificado en vivo end-to-end: formateo de teléfono en tiempo real (`3121234567` → `+57 312 123 4567`); `empresa002@demo.com` (sin relación previa con Santiago) visitando `/portfolio/santiago-bustamante` → fila nueva en `profile_views` confirmada por SQL directo → el candidato ve "Vistas de tu perfil: 1" en su dashboard con el detalle de qué empresa lo vio (nombre + logo, vía `GET /profile/views`).
**Pendientes:** Autocompletado/formato de campos no se extendió a todos los formularios del proyecto (educación, proyectos, registro de candidato, etc.) — solo a los de mayor uso. Vinculación de Joaquín/análisis de CV con IA real (Claude) — pedido explícitamente pospuesto por el usuario para una sesión futura, ver `NEXT_STEPS.md`.
