# Próximos pasos — TalentBridge V3

Estado al 2026-07-17. Ordenado por prioridad. Ver `PHASES.md` para el plan completo por fases.

| # | Tarea | Prioridad | Estado | Módulo | Riesgo | Recomendación | Siguiente acción |
|---|---|---|---|---|---|---|---|
| 12 | ~~Rediseño del flujo de agregar habilidades~~ **Ya hecho** — corregido 2026-07-17: catálogo por categorías + búsqueda rápida + selección múltiple + agregado en batch + edición de nivel en línea, elegido por el usuario entre 3 opciones. Ver `CHANGELOG.md`, BUG-013/BUG-014 en `BUGS_AND_FIXES.md`. | — | **Completo** | Frontend — `skills` / Backend — `portfolio-service` | — | Ninguna | Ninguna |
| 1 | ~~Confirmar y limpiar las habilidades del perfil demo~~ **Ya hecho** — corregido 2026-07-16: el usuario autorizó explícitamente. El conteo real al momento de limpiar era 13 (no ~150, esa cifra ya estaba desactualizada), mezcla del seed original + agregados manuales de auditorías + un artefacto de prueba. Repuestas las 6 habilidades del seed (`seed-santiago-profile.ts`). Detalle y respaldo en `CHANGELOG.md`/`DECISIONS.md`. | — | **Completo** | Backend/datos | — | Ninguna | Ninguna |
| 2 | ~~Logos de empresa realistas para el seed~~ **Ya hecho, corregido de nuevo el 2026-07-17 (continuación):** la verificación anterior (misma fecha) había confirmado que existían 11 SVG y que `logoUrl` estaba poblado, pero no notó que los 11 archivos eran una sola silueta abstracta genérica repartida sin variación entre las 61 empresas (5-6 por archivo) — el usuario lo notó al ver una "M" en un cuadrado morado repetida y pidió cambiarlo. Nuevo script `generate-company-logos.ts` genera una marca única por empresa (22 siluetas × 22 colores, sin repetir combinación, con preferencia temática por sector). 61 empresas, 61 logos distintos ahora. | — | **Completo** | Frontend + Backend/datos | — | Ninguna | Ninguna |
| 3 | ~~Datos de seed incompletos: `workload: null`~~ **Ya hecho** — corregido 2026-07-17: era sistémico (las 66 ofertas publicadas de toda la base, no solo de `empresa001@demo.com`). Backfill corrido vía `npm run backfill:job-workload` (`BACKEND/prisma/backfill-job-workload.ts`), 68 ofertas actualizadas. | — | **Completo** | Backend/datos | — | Ninguna | Ninguna |
| 4 | Verificación visual completa en navegador real (Fase 11) | Media | Parcial — ampliado 2026-07-17: además de lo ya auditado, se verificaron en vivo registro candidato, registro empresa, editar oferta (con datos precargados correctos, incluida la jornada del backfill) y envío/recepción real de un mensaje de chat (encontró y corrigió BUG-012) | Frontend, todos los módulos | Bajo | Quedan sin auditar en vivo: accesibilidad (lector de pantalla/teclado), tablet, doble postulación (409), rechazo de archivo CV >5MB | Recorrer los módulos restantes de `TESTING_CHECKLIST.md` si se quiere cobertura 100% |
| 5 | Adopción de `Card` compartido | Baja | Diferido a propósito (ver `DECISIONS.md`) | Frontend, ~14 componentes | Medio si se hace mecánicamente sin verificación visual por pantalla | Encarar como tanda dedicada, un componente a la vez, con captura antes/después | No iniciar sin pedirlo explícitamente — no es un bug, es deuda cosmética documentada |
| 6 | `.view-btn`/`.btn-clear` de `company-candidates` sin variante de `appButton` que calce | Baja | Abierto, evaluado y descartado por ahora | Frontend — `company-candidates` | Bajo | Si se quiere unificar, la opción real es agregar una 5ª variante al sistema de botones (`soft`/`outline-danger`), no forzar las 4 existentes | Evaluar si vale la pena agregar variante nueva al sistema de diseño, o dejarlo bespoke permanentemente |
| 7 | Íconos de navegación de `company-shell`/`app-shell` sin migrar a `appButton` | Baja | Evaluado y descartado — patrón visual distinto, no aplica | Frontend — shells | N/A | No es una tarea pendiente real, dejar documentado para que no se re-evalúe de cero | Ninguna — cerrado como "no aplica" |
| 8 | Sin suite de tests automatizados en microservicios de negocio | Media | No iniciado | Backend, todos los servicios | Medio a largo plazo (regresiones silenciosas) | Fuera del alcance del plan UX actual — evaluar si el proyecto académico lo requiere antes de invertir tiempo acá | Preguntar al usuario si es un requisito de la entrega |
| 9 | `environment.ts` / URL de API configurable en frontend | Baja | No iniciado | Frontend | Bajo mientras todo corra local | Cada `core/services/*.ts` tiene `http://localhost:3000/api` hardcodeado — no bloquea nada hoy, sí bloquearía un deploy fuera de local | Solo si el proyecto se va a desplegar en un dominio real |
| 10 | Titlecase imperfecto en siglas técnicas (chips de habilidades) | — | **Cerrado por decisión** 2026-07-17: se deja como está a propósito (ver BUG-011) | Frontend | N/A | "NestJS"/"PostgreSQL"/"HTML5" se ven como "Nestjs"/"Postgresql"/"Html5" tras el fix de mayúsculas — mejor que todo-minúscula; una tabla de excepciones de siglas es desproporcionada para el beneficio cosmético que da | No reabrir salvo pedido explícito |
| 11 | Un solo commit al final de todo el plan UX (regla ya acordada previamente) | — | Pendiente hasta cerrar Fases 0.1/9/11 | Git | Alto si se hace a destiempo (mezclaría trabajo incompleto) | No commitear hasta que el usuario lo pida explícitamente, aunque el plan esté "casi" terminado | Esperar instrucción explícita de commit |
| 13 | Análisis de CV con IA real (Claude) y Joaquín como asistente conversacional real (no reglas fijas) | Media | **Pospuesto a propósito por el usuario** 2026-07-17 — pidió explícitamente "esto lo haremos más adelante, primero haz lo demás" | Backend — `portfolio-service` (`cv.service.ts`), `assistant-service` | Medio — implica costo real por uso de API y necesita una API key de Anthropic del usuario | No iniciar sin que el usuario lo pida en ese momento puntual | Cuando se retome: pedir la API key (o cómo prefiere proveerla — variable de entorno, no hardcodeada), diseñar el prompt de análisis de CV y el rol/tono de Joaquín, y decidir si hay un límite de uso/costo a cuidar |

## Warnings de build conocidos (no bloquean, no son nuevos)

`ng build` del frontend deja warnings de presupuesto de bundle (`budget.initial` y varios `*.component.scss` individuales excediendo 6kB) que ya existían antes de esta sesión y no crecieron por el trabajo hecho acá — están listados para que un build futuro con un warning **nuevo** sea fácil de distinguir de los preexistentes. Ver el detalle completo corriendo `ng build` — la lista cambia con cada commit y no vale la pena fijarla acá como texto estático.
